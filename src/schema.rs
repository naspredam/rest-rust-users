table! {
    users (id) {
        id -> Integer,
        first_name -> Varchar,
        last_name -> Varchar,
        phone -> Varchar,
        active -> Bool,
        created_at -> Datetime,
        updated_at -> Datetime,
    }
}
